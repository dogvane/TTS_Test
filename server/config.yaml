# TTS 测试网关全局配置

# 网关服务端口
host: "0.0.0.0"
port: 9000

# 已注册的引擎列表（对应 tts/ 下的目录名）
engines:
  - name: VoxCPM2
    conda_env: voxcpm
  - name: MOSS-TTSD
    conda_env: moss-tts
  - name: qwen3-tts
    conda_env: qwen3-tts
  - name: higgs-audio
    conda_env: ""
